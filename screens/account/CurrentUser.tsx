import * as React from 'react'
import { View } from 'react-native';

interface ICurrentUser {
    navigation: any;
}

const CurrentUser: React.FunctionComponent<ICurrentUser> = ({navigation}) => {
    return(
        <View></View>
    )
}

export default CurrentUser;